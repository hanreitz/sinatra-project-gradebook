<div class="container">
  <div class="col-md-8 col-md-offset-2">
    <h2>Edit <%= @course.name %></h2>
    <p>Please use the information below to edit the course. Course name is required.</p>

    <div class="content">
      <form action='/courses/<%= @course.id %>' method="POST">
        <input type="hidden" name="_method" value="PATCH">

        <label for="course[name]">Course Name:</label>
        <input type="text" name="course[name]" value="<%= @course.name %>"><br><br>

        <p>To add or remove existing students, please check below:</p>
        <% Student.all.each do |student| %>
          <input type="checkbox" name="course[student_ids][]" value="<%= student.id %>" <%= 'checked' if @course.students.include?(student) %>><%= student.name %><br>
        <% end %>
        <br>
        <p>To add a new student to this course, please fill in the following. Name is required.</p>
        <label for="student[name]">Student Name:</label>
        <input type = "text" name="student[name]"><br>
        <label for="student[grade_level]">Grade Level:</label>
        <input type="text" name="student[grade_level]"><br>
        <label for="student[contact_name]">Contact Name:</label>
        <input type="text" name="student[contact_name]"><br>
        <label for="student[contact_phone]">Contact Phone:</label>
        <input type="text" name="student[contact_phone]"><br><br>

        <input type="submit" value="Edit Course"><br>
      </form><br>

    </div>

  </div>
</div>